{% extends 'administration/base.html.twig' %}
{% trans_default_domain "administration" %}

{% block title %}{% trans %}Edit category{% endtrans %}{% endblock %}

{% block body %}
    <div class="dashboard-header">
        <h1 class="dashboard-title">{% trans %}Edit category{% endtrans %}</h1>
        <a class="basic-button" href="{{ path('category_index') }}">{% trans %}Back to list{% endtrans %}</a>
    </div>

    {{ include('category/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('category/_delete_form.html.twig') }}
{% endblock %}
